import React from 'react';
import Policy from '../../containers/Policy';

const PolicyView = () => {
    return (
        <div>
            <Policy />
        </div>
    );
};

export default PolicyView;
