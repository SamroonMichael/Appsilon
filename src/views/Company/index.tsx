import React from 'react';
import Company from '../../containers/Company';

const CompanyView = () => {
    return (
        <div>
            <Company />
        </div>
    );
};

export default CompanyView;
